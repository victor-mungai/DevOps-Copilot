# DevOps Copilot Onboarding UI

React + Vite UI for onboarding.

Run
1. npm install
2. npm run dev

Open
- http://127.0.0.1:8000 (gateway, recommended)
- http://127.0.0.1:8002 (UI dev server)

API Base
- Default: current browser origin
- Override: set `VITE_API_BASE` (e.g. `http://127.0.0.1:8000`)
